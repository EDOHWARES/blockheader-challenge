export interface PalindromeResult {
    isPalindrome: boolean
    cleanedText: string
    originalText: string
  }
  
  export interface PalindromeCheckerProps {
    placeholder?: string
    className?: string
  }
  